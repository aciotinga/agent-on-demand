Transform the following source output data into input data for the target capsule.

**Source Output Data:**
```json
{source_output}
```

**Target Capsule:** {target_capsule}

**Target Capsule Input Schema:**
```json
{target_schema}
```

**Field Mappings:**
{mapping_instructions}

**Transformation Instructions:**
{transformation_instructions}

**Your Task:**
1. Analyze the source output structure
2. Review the target capsule's input schema requirements
3. Apply the field mappings (if provided)
4. Follow the transformation instructions (if provided)
5. Generate a JSON object that matches the target schema exactly

Return ONLY the transformed JSON object that matches the target schema. Do not include any explanation or additional text - just the JSON object.
